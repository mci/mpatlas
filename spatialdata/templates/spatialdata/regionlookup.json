{
    "regions": [{% for region in region_list %}
        {"name": "{{ region.name }}", "country": "{% if region.sovereign %}{{ region.sovereign }}{% else %}{{ region.country }}{% endif %}", "id": "{{ region.pk }}", "url": "{{ region.get_absolute_url }}", "area_km2": "{{ region.marine_area }}",  "mpas": "{{ region.mpas|length }}", "percent_in_mpas": "{{ region.mpa_percent }}", "percent_no_take": "?" }{% if not forloop.last %},{% endif %}{% endfor %}
    ]
}
